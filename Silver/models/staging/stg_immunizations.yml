version: 2

models:
  - name: stg_immunizations
    description: "HL7 Immunization events aligned with CVX coding"

    # tests:
    #   - dbt_utils.unique_combination_of_columns:
    #       combination_of_columns:
    #         - patient_id
    #         - encounter_id
    #         - immunization_datetime
    #         - code

    columns:
      - name: immunization_datetime
        description: "Timestamp of the immunization event"
        tests:
          - not_null

      - name: patient_id
        description: "Unique identifier for the patient"
        tests:
          - not_null

      - name: encounter_id
        description: "Identifier for the related encounter"
        tests:
          - not_null

      - name: code
        description: "Immunization code (CVX)"
        tests:
          - not_null

      # - name: cost
      #   tests:
      #     - not_null
