version: 2

models:
  - name: stg_supplies
    description: "HL7 Supplies records aligned with SNOMED-CT"

    # tests:
    #   - dbt_utils.unique_combination_of_columns:
    #       combination_of_columns:
    #         - patient_id
    #         - encounter_id
    #         - code
    #         - date

    columns:
      - name: date
        description: "Supply date"
        tests:
          - not_null

      - name: patient_id
        description: "Unique identifier for the patient"
        tests:
          - not_null

      # - name: encounter_id
      #   tests:
      #     - not_null

      - name: code
        description: "Supply code"
        tests:
          - not_null

      - name: description
        description: "Supply description"
        tests:
          - not_null

      - name: quantity
        description: "Quantity of supply used"
        tests:
          - not_null
